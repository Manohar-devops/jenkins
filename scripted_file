pipeline {
    agent {
        label 'java-agent-slave'
    }
    stages {
        stage ('Build') {
            steps {
                echo "Maven Version with default java version"
                sh 'mvn --version'
            }  
        }
    }
}
